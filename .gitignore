# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/

# Downloaded upstream archives and extracted tables (multi-GB; re-fetch with `make`)
data/raw/

# Derived database: rebuildable from data/law in ~40s with `make db`.
# Committed to git only if you explicitly want a queryable copy in the repo.
data/leginfo.sqlite
data/leginfo.sqlite-wal
data/leginfo.sqlite-shm

# Self-help sqlite database is rebuildable from pages.jsonl.gz.
data/selfhelp/selfhelp.sqlite
data/selfhelp/selfhelp.sqlite-wal
data/selfhelp/selfhelp.sqlite-shm
# Raw page cache is environment-specific (keep the seed, but ignore ad-hoc caches).
# data/selfhelp/cache/_index.jsonl is committed.

# Generated crawlable site: rebuild with `make html`.
dist/
autorag/

# Scratch
probe_out/
.DS_Store
