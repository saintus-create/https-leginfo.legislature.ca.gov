name: Validate legislative corpus

on:
  workflow_dispatch:
  schedule:
    - cron: '17 8 * * 0'

permissions:
  contents: read

concurrency:
  group: validate
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Validate compressed JSONL corpus
        run: node scripts/validate-corpus.mjs
      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
      - name: Install dependencies
        run: bun install --no-progress
      - name: Test research orchestrator
        run: bun test tests/orchestrator.test.ts
      - name: Build and verify deployed corpus artifact
        run: bash scripts/build-site.sh
